# External port for the homepage dashboard (container stays on 3000)
APP_PORT=3003

# Live service widgets (leave blank to disable the widget)
# AdGuard Home widget — dashboard login credentials
ADGUARD_USER=
ADGUARD_PASS=
# Uptime Kuma widget — the slug of a published status page (Kuma > Status Pages)
KUMA_SLUG=
